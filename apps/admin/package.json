{
  "name": "@operoz/admin",
  "version": "0.1.0",
  "private": true,
  "description": "Painel administrativo 'god-mode' — equivalente ao apps/admin do projeto anterior. Ver README.md.",
  "scripts": {
    "dev": "echo 'apps/admin: ainda não implementado — ver README.md' && exit 0",
    "build": "echo 'apps/admin: ainda não implementado' && exit 0",
    "lint": "echo 'apps/admin: ainda não implementado' && exit 0",
    "typecheck": "echo 'apps/admin: ainda não implementado' && exit 0",
    "test": "echo 'apps/admin: ainda não implementado' && exit 0"
  }
}
